# Required
version: 2

# Image to use
build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

sphinx:
   configuration: docs/conf.py

formats:
  - pdf

# Configuration
python:
  install:
    - requirements: docs/requirements.txt
